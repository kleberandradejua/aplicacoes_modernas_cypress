module.exports = {
  viewportWidth: 1280,
  viewportHeight: 720,

  //"defaultCommandTimeout": 1000,

  e2e: {
    setupNodeEvents(on, config) {
      // implement node event listeners here
    },
  },
};
